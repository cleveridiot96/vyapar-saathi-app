import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function LocationTransferPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Location Transfer</CardTitle>
        <CardDescription>Facilitates the transfer of stock between different warehouses.</CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">This feature is under development. Check back soon!</p>
      </CardContent>
    </Card>
  );
}
